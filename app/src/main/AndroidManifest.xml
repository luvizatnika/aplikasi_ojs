<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.luvizatnika1501512.utsmobprog.myojs">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />


    <application
        android:name=".service.AppController"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning">
        <activity android:name=".view.MainActivity">
        </activity>
        <activity android:name=".view.RegisterActivity"
            android:label="@string/register"/>
        <activity android:name=".view.MainMenu"
            android:label="@string/main_menu"/>
        <activity
            android:name=".view.tambah_data"
            android:label="@string/masukan_data" />
        <activity
            android:name=".view.edit_data"
            android:label="@string/edit_data" />
        <activity
            android:name=".view.Deskripsi"
            android:label="@string/detail_jurnal" />
        <activity
            android:name=".view.Profil"
            android:label="@string/profil"></activity>
        <activity android:name=".view.Content" />
        <activity android:name=".view.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat"/>
    </application>

</manifest>